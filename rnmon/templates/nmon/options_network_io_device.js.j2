
let network_io_device_{{ nmon_id }} = echarts.init(document.getElementById('{{ nmon_id }}_NETWORK_IO_Device'));
network_io_device_{{ nmon_id }}.setOption(
{
    title: {
        text: '{{ nmon_name }}',
        left: 'center',
        top: 'top',
    },
    legend: {
        top: '30',
    },
    tooltip: {
        trigger: 'axis',
        axisPointer: {
            type: 'cross',
        },
    },
    xAxis: { 
        type: 'time',
        axisLabel: {
            rotate: '90',
            formatter: '{hh}:{mm}',
        },
    },
    yAxis: [
        { 
        },
    ],
    series: [
        {% for net in series_data -%}
            { 
                name: '{{ net.0 }}',
                type: 'line',
                areaStyle: {},
                stack: '1',
                data: [
                    {% for data in net.1 -%}
                        [ '{{ data.x }}', {{ data.y }} ],
                    {% endfor -%}
                ],
            },
        {% endfor -%}
    ],
}
);